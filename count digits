
#include <bits/stdc++.h>
using namespace std;

int countDigit(int n) {
    if (n/10 == 0)
        return 1;
    return 1 + countDigit(n / 10);
}

int main() {
   int n = 58964;
    cout << countDigit(n);
    return 0;
}
